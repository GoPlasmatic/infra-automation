# Upstream definitions - loaded first to ensure availability
# Ghost CMS upstream
upstream ghost_upstream {
    server ghost:2368;
}

# Sandbox upstream
upstream sandbox_upstream {
    server sandbox:3000;
}

# Reframe API upstream
upstream reframe_upstream {
    server reframe:3000;
}

# Grafana upstream
upstream grafana_upstream {
    server grafana:3000;
}