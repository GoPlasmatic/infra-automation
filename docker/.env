# Domain configuration
DOMAIN_NAME=goplasmatic.io

# Ghost CMS configuration
GHOST_MYSQL_ROOT_PASSWORD=${GHOST_MYSQL_ROOT_PASSWORD}
GHOST_MYSQL_DATABASE=ghost_production
GHOST_MYSQL_USER=ghost
GHOST_MYSQL_PASSWORD=${GHOST_MYSQL_PASSWORD}

# Mail configuration (update these with your mail service)
# Mailgun SMTP configuration
MAIL_SERVICE=Mailgun
MAIL_USER=postmaster@goplasmatic.io
MAIL_PASS=${MAILGUN_SMTP_PASSWORD}
MAIL_FROM=noreply@goplasmatic.io

# Backup configuration
BACKUP_RETENTION_DAYS=7

# Monitoring configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}

# Azure Container Registry configuration
ACR_URL=${ACR_URL}
